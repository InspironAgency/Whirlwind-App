{
  "name": "uv-app",
  "version": "1.0.0",
  "description": "Ultraviolet instance",
  "type": "module",
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=16.0.0"
  },
  "scripts": {
    "prestart": "esbuild public/index.js --bundle --minify --sourcemap --loader:.html=text --outfile=public/index.min.js && esbuild public/style.css --bundle --minify --sourcemap --loader:.html=text --outfile=public/style.min.css",
    "start": "node src/index.js"
  },
  "keywords": [
    "proxy"
  ],
  "author": "",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@mercuryworkshop/bare-mux": "^2.0.4",
    "@mercuryworkshop/epoxy-transport": "^2.1.5",
    "@titaniumnetwork-dev/ultraviolet": "^3.2.6",
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "wisp-server-node": "^1.1.3",
    "ws": "^8.18.0",
    "esbuild": "0.23.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^2.8.8"
  }
}
